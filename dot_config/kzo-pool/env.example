# Copy to ~/.config/kzo-pool/env and fill in.
# The real file is gitignored — it holds a live token.
#
# Fine-grained or classic PAT with repo scope, used by kzo-pool slots for
# git operations against the configured repos.
export GH_TOKEN=
